# API do cardápio (Laravel)
VITE_API_URL=http://localhost:8000/api

# Google Maps — Places Autocomplete + mapa no checkout
# Restrinja a chave por HTTP referrer no Google Cloud Console
VITE_GOOGLE_MAPS_API_KEY=
